using Markdig;
namespace YaRpgApp.Web.Models
{
    public static class Markdown
    {
        // public static string Parse(string markdown,
        //     bool usePragmaLines = false,
        //     bool forceReload = false)
        // {
        //     if (string.IsNullOrEmpty(markdown))
        //         return "";
        //     \var parser = new  Markdig.Parsers. (usePragmaLines: usePragmaLines, force: forceLoad); //  MarkdownParserFactory.GetParser(usePragmaLines, forceReload);
        //     return parser.Parse(markdown);
        // }
        //
        // public static HtmlString ParseHtmlString(string markdown,
        //     bool usePragmaLines = false,
        //     bool forceReload = false)
        // {
        //     return new HtmlString(Parse(markdown, usePragmaLines, forceReload));
        // }
    }
}